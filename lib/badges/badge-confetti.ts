/** Palette for badge unlock celebration bursts. */
export function badgeConfettiColors(categoryColor?: string): string[] {
  return [categoryColor ?? '#E65100', '#F9A825', '#2E7D32', '#90CAF9', '#FF6D00'];
}
